{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "web-related": "npm run server:browser-sync & npm run watch:sass",
    "server:browser-sync": "browser-sync start --config bs-config.js",
    "watch:sass": "sass --watch --no-source-map ./sass:./css",
    "watch:pug": "pug --watch -P pug/source/** -o pug/output"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.27.5",
    "pug": "^3.0.2",
    "pug-cli": "github:pugjs/pug-cli#master",
    "sass": "^1.43.2"
  }
}
